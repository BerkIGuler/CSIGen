# CSIGen

Channel generation package using Sionna RT for wireless communication simulations.

## TODO:
1) Most sampled users are on the edges of the map. Fix this. We can drop the users that are less than epsilon close to the outer edges. 
2) Allow saving path objects to allow the user to post-process path information.